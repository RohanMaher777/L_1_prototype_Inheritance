/**
 * Using let and const we define the variable inside the block and that is 
 * only accessiable inside that scope only is called the block scope
 */

{
    let a = 5
}
console.log(a) // a is not defined